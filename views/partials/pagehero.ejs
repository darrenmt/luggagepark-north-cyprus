<section class="page-hero">
  <div class="wrap">
    <%- include('crumbs') %>
    <h1><%= h1 %></h1>
    <% if (typeof lede !== 'undefined' && lede) { %><p class="lede" style="margin:0"><%= lede %></p><% } %>
  </div>
</section>
